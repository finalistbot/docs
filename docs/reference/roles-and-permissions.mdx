---
sidebar_position: 2
---

# Roles and permissions

Finalist has several independent role systems. They don't overlap, and confusing them is the
usual source of "why can't I do this?".

## In a team

| Role | Can |
|------|-----|
| **Captain** | Register and unregister the team, pick the lineup, approve join requests, create and revoke invites, kick members, rename and delete the team. |
| **Member** | Play. Leave the team. |
| **Substitute** | Play, if the scrim allows substitutes. |

One captain per team, and the captain cannot leave, only delete.

## In an organization

| Role | Can |
|------|-----|
| **Owner** | Everything, including transferring the org to another member. Exactly one at a time. |
| **Admin** | Every org action except transferring ownership: scrims, slots, results, members, invites, bans, Discord. |
| **Moderator** | A limited set of moderation actions. |

Membership is by invitation only. Invites expire after 7 days.

An owner can hand the org to any existing member; doing so leaves the outgoing owner in the
org as an admin. Every organization has an owner from the moment it is created — the person
who created it on the dashboard.

## In a tournament

Tournaments layer their own **staff roles** on top of org membership, so you can trust a
referee for one weekend without making them a moderator of your whole organization.

| Role | Can |
|------|-----|
| **Owner** | Everything. Conferred at creation, never granted. |
| **Admin** | Reconfigure the event, its stages, prizes and staff. |
| **Referee** | Run matches: check-in, results, disputes. |
| **Caster** | Read-only. |

See [Running a tournament](../tournaments/running#staff).

## In a scrim's chat

Chat resolves your standing per scrim, from your stake in *that* scrim. Ranks are ordered:
a higher rank clears every gate a lower one does.

| Rank | Who |
|------|-----|
| **organizer** | The scrim's creator, or an org owner/admin. |
| **moderator** | An org moderator. |
| **player** | Your team holds a slot. |
| **waitlist** | Your team is registered, not slotted. |
| **viewer** | Signed in, no stake in this scrim. |

Organizers and moderators bypass the posting rules (mode, slow mode, repeat-blocking)
entirely. See [Moderating scrim chat](../scrims/chat-moderation).

## Moderation thresholds

| Action | Needs |
|--------|-------|
| Work the [report queue](../organizers/reports) — claim, triage, resolve | Moderator |
| Moderate chat in one scrim | Moderator |
| Org-wide chat sanctions, player and team bans | Admin |

## In Discord

The bot's `/host` commands check your **organization** role, not your Discord permissions.

This surprises people in both directions:

- An org admin can use `/host` **without** Discord's Manage Server permission.
- A Discord server administrator who isn't in the organization **cannot** use `/host` at all.

Discord's Manage Server permission is needed only for the thing Discord itself governs:
adding the bot to the server. Every other check is a platform check.

Meanwhile most of `/scrim`, `/tournament` and `/team` need nothing at all — browsing and
reading work without an account. `/me`, entering a scrim and checking in need a
[linked account](../discord/link-account); `/org` and `/host` need the server
[connected to an organization](../discord/connect-server).

## Bans

Bans belong to an organization. A team or player banned by one org can still register with
every other org. Being banned from a Discord server and being banned from an organization
are unrelated.
